多层感知器(MLP): mlp; multilayer perceptron; multiple layers; non-linear activation
循环神经网络(RNN): rnn; recurrent-neural-network; lstm; fully recurrent; elman networks; jordan networks; hopfield; echo state; recursive neural network; gru
卷积神经网络(CNN): cnn; conv-neural-network; convolution; convnet; receptive field; 